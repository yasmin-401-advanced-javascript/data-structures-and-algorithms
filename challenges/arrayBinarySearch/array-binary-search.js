"use strict";

function binarySearch(arr, num) {
  for (let i = 0; i < arr.length; i++) {
    if (num === arr[i]) {
      return i;
    }
  }
  let inCase = -1;
  if (inCase) {
    return inCase;
  }
}

module.exports = binarySearch;
